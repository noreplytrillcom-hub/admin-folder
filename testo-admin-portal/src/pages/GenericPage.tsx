import { Plus, Download, MoreHorizontal } from 'lucide-react'
import { PageIntro, TableToolbar, Status } from '../components/UI'

type Props={title:string;text:string;columns:string[];rows:string[][];action?:string}
export default function GenericPage({title,text,columns,rows,action='Create new'}:Props){return <><PageIntro title={title} text={text} action={<div className="action-pair"><button className="secondary-btn"><Download size={16}/> Export</button><button className="primary-btn"><Plus size={16}/> {action}</button></div>}/><div className="panel"><TableToolbar/><div className="table-wrap"><table><thead><tr>{columns.map(c=><th key={c}>{c}</th>)}<th></th></tr></thead><tbody>{rows.map((row,ri)=><tr key={ri}>{row.map((cell,ci)=><td key={ci}>{ci===row.length-1&&['Active','Paid','Open','Successful','Enabled','Sent'].includes(cell)?<Status>{cell}</Status>:ci===row.length-1&&['Pending','Trial','Processing'].includes(cell)?<Status tone="yellow">{cell}</Status>:ci===row.length-1&&['Failed','Refunded','Disabled','Closed'].includes(cell)?<Status tone="red">{cell}</Status>:<>{ci===0?<strong>{cell}</strong>:cell}</>}</td>)}<td><button className="icon-btn"><MoreHorizontal size={17}/></button></td></tr>)}</tbody></table></div></div></>}
