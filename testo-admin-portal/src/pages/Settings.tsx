import { Save, ShieldCheck, KeyRound, Database, Mail, Globe } from 'lucide-react'
import { PageIntro } from '../components/UI'
const sections=[['General settings','Business name, locale and portal preferences',Globe],['Authentication & MFA','Session rules, SSO and multi-factor authentication',ShieldCheck],['API keys','Manage platform and organization API credentials',KeyRound],['Supabase connection','Database, storage, realtime and edge functions',Database],['Email delivery','Transactional templates and sender configuration',Mail]]
export default function Settings(){return <><PageIntro title="Settings" text="Configure your Testo AI platform, security and integrations." action={<button className="primary-btn"><Save size={16}/> Save changes</button>}/><div className="settings-layout"><aside className="settings-nav">{sections.map((s,i)=>{const Icon=s[2] as typeof Globe;return <button className={i===0?'active':''} key={s[0] as string}><Icon size={18}/><span><strong>{s[0] as string}</strong><small>{s[1] as string}</small></span></button>})}</aside><div className="panel settings-form"><h3>General settings</h3><p>Basic information used throughout the administration portal.</p><div className="form-grid"><label>Business name<input defaultValue="Testo AI"/></label><label>Support email<input defaultValue="support@testo.ai"/></label><label>Default currency<select><option>GBP (£)</option><option>USD ($)</option></select></label><label>Default timezone<select><option>Europe/London</option><option>Asia/Kolkata</option></select></label><label className="full">Portal URL<input defaultValue="https://admin.testo.ai"/></label></div><div className="setting-switch"><div><strong>Maintenance mode</strong><span>Temporarily prevent customer access to the platform.</span></div><button className="switch"><i/></button></div><div className="setting-switch"><div><strong>Allow organization self-signup</strong><span>Let approved companies start a trial from the public website.</span></div><button className="switch on"><i/></button></div></div></div></>}
